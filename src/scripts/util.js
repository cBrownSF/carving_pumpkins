const Util = {
  getDist(x1,y1, x2,y2) {
    return Math.sqrt(
      Math.pow(x1 - x2, 2) + Math.pow(y1 - y2, 2)
    );
  },
  // isCollided(getDist){
  //  return getDist === 0;
  // }

//if (isCollided === true){
  // this.velX = velX * -1
    // this.velY = velY * -1
// } else{
  // this.velX;
  // this.velY;
// }
}
